# Bit
wget -q $(wget -qO- https://api.github.com/repos/chriswalz/bit/releases|grep browser_download_url|grep linux_amd64.tar.gz|head -n1|cut -d \" -f4)
tar -xzvf bit*linux_amd64.tar.gz
mkdir -pv bit/DEBIAN bit/usr/bin